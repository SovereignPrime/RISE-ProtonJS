//const { contextBridge } = require('electron')
//const rise = require('risejs');
//import IPFS from 'ipfs-http-client';
//const node = IPFS.create();

//contextBridge.exposeInMainWorld('RISE', rise);
/* 
rise.rise.on('ready', () => {
});
*/
/*node.id().then((id) =>
    console.log(id));
window.RISE = node;
*/

